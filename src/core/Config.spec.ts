describe('Config', () => {
  it('should load the a configuration from file system', () => {
    
  });

  it('should getPath', () => {
    
  });

  it('should get a configuration with using the key', () => {
    
  });

  it('should load configuration from cache', () => {
    
  });

  it('should get configuration from cache', () => {
    
  });
});